{
    "version": "2.0.0",
    "tasks": [
      {
        "label": "Run",
        "command": "${env:ProcessingExeDir}\\processing-java",
        "type": "shell",
        "args": [
          "--force",
          "--sketch=${workspaceFolder}\\${relativeFileDirname}",
          "--run"
        ],
        "group": {
          "kind": "build",
          "isDefault": true
        },
        "presentation": {
          "reveal": "always",
          "panel": "new"
        }
      }
    ]
}
